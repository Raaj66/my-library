export enum ModalSize {
  Medium = 'md',
  Large = 'lg',
  ExtraLarge = 'xl',
}

export enum ModalType {
  Action = 'action',
  FileUpload = 'fileUpload',
  History = 'history',
  MultiStep = 'multiStep',
  Warning = 'warning',
  Custom = 'custom',
}
