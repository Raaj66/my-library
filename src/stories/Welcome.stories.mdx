import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import LinkTo from '@storybook/addon-links/react';

<Meta title="Introduction" parameters={{
  id: 100,
  viewMode: 'docs',
  previewTabs: {
    canvas: { hidden: true }
  }
}}/>

# Welcome to the ICN storybook!

This storybook aims to hold all the components that the application
use to facilitate creating re-usable and common components

Take a look at an <LinkTo kind='Components/IcnButton'>example</LinkTo>
